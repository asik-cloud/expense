- name: Conditions
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Check if the user expense exists or not
    ansible.builtin.command: id expense
    register: user
    ignore_errors: yes
  - name: create user
    ansible.builtin.command: useradd expense
    when: user.rc != 0