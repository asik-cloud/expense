apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: first-cluster
  labels:
    prj: expense
    env: dev
  region: us-east-1

managedNodeGroups:
- name: ng 
  instanceType: t3.micro
  desiredCapacity: 3
  spot: true