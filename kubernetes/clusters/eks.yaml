apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: Cluster-1
  region: us-east-1

managedNodeGroups:
  - name: Cluster-ng
    instanceType: t3.micro
    desiredCapacity: 3
    spot: true